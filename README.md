# Muttertag Rose

A gift page for Angela — a canvas animation that progressively reveals a rose photograph over ~8 seconds, ending with a message.

## Run

Open `index.html` directly in a browser, or serve locally:

```bash
python3 -m http.server
```

`rose.jpeg` must be in the same directory.
